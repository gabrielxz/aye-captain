{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Ship Command Protocol v1",
  "description": "The contract between the LLM translator and the authoritative game server. The LLM receives natural language from the captain and emits one or more Command objects. The server validates and executes them on its tick. Unknown fields are rejected. This schema is also the source of truth for the LLM's tool/function definitions.",

  "definitions": {

    "command": {
      "description": "Envelope for every command the LLM emits.",
      "type": "object",
      "required": ["verb", "params"],
      "properties": {
        "verb": {
          "enum": [
            "set_thrust",
            "set_heading",
            "fire_laser",
            "fire_missile",
            "deploy_decoy",
            "set_standing_order",
            "query"
          ]
        },
        "params": { "type": "object" },
        "acknowledgement": {
          "type": "string",
          "description": "Short first-mate flavor line the client speaks/displays back to the captain, e.g. 'Aye, coming to 090.' Generated by the LLM, ignored by the server."
        }
      }
    },

    "set_thrust": {
      "description": "Set main drive output. Thrust accelerates the ship along its current facing. Persists until changed. NOTE (future): thrust level will drive sensor signature; 0 = dark and drifting.",
      "type": "object",
      "required": ["percent"],
      "properties": {
        "percent": {
          "type": "number",
          "minimum": 0,
          "maximum": 100
        }
      },
      "examples": [
        { "percent": 100 },
        { "percent": 0 }
      ]
    },

    "set_heading": {
      "description": "Rotate the ship at its fixed turn rate toward a goal orientation. A new set_heading replaces any turn in progress. Does not affect velocity vector (Newtonian drift).",
      "type": "object",
      "required": ["mode"],
      "properties": {
        "mode": {
          "enum": ["relative", "absolute", "target"]
        },
        "degrees": {
          "type": "number",
          "description": "REQUIRED for mode=relative (positive only; combine with direction) and mode=absolute (0-359, where 0 = map north, clockwise)."
        },
        "direction": {
          "enum": ["port", "starboard"],
          "description": "REQUIRED for mode=relative. port = counterclockwise, starboard = clockwise. The LLM maps 'left'->port, 'right'->starboard."
        },
        "target": {
          "enum": ["enemy_ship", "nearest_missile", "nearest_decoy", "nearest_contact"],
          "description": "REQUIRED for mode=target. Server resolves to a bearing each tick, so the ship tracks a moving target until the order is replaced. 'enemy_ship' resolves to last-known position if the enemy is not currently on sensors."
        }
      },
      "examples": [
        { "mode": "relative", "direction": "starboard", "degrees": 30 },
        { "mode": "absolute", "degrees": 90 },
        { "mode": "target", "target": "nearest_missile" }
      ]
    },

    "fire_laser": {
      "description": "Fire the laser straight along the ship's current facing. Hitscan: hits the FIRST object within beam_width_degrees of the boresight, in range (ship, missile, or decoy). Subject to cooldown; server rejects with 'recharging' if fired early. No ammo limit.",
      "type": "object",
      "properties": {},
      "examples": [ {} ]
    },

    "fire_missile": {
      "description": "Launch a missile straight ahead. After launch_delay ticks, the seeker locks the strongest signature inside its forward acquisition cone (ships and decoys emit signature; future: signature scales with target thrust). Missile flies at fixed speed, can be destroyed by lasers, and detonates on proximity. Rejected if magazine is empty.",
      "type": "object",
      "properties": {},
      "examples": [ {} ]
    },

    "deploy_decoy": {
      "description": "Eject a decoy that inherits the ship's current velocity plus a small random drift. Emits a hot signature for decoy_lifetime_seconds, attracting missile seekers and (future) appearing as a sensor contact. Rejected if decoy supply is empty.",
      "type": "object",
      "properties": {},
      "examples": [ {} ]
    },

    "set_standing_order": {
      "description": "Register a conditional order: WHEN condition THEN actions. The server evaluates conditions every tick; when one fires, its actions are executed as if the captain had just issued them, and the order is consumed (one-shot) unless repeat=true. Max active standing orders: server-configured (suggest 3 for crewed/NeWk play, 6 for solo captains).",
      "type": "object",
      "required": ["condition", "actions"],
      "properties": {
        "label": {
          "type": "string",
          "description": "Short human-readable name the crew uses to reference or cancel it, e.g. 'missile defense'."
        },
        "condition": { "$ref": "#/definitions/condition" },
        "actions": {
          "type": "array",
          "minItems": 1,
          "maxItems": 3,
          "items": { "$ref": "#/definitions/command" },
          "description": "Commands to execute in order when the condition fires. set_standing_order is NOT allowed as a nested action (no recursive orders in v1)."
        },
        "repeat": {
          "type": "boolean",
          "default": false,
          "description": "If true, the order re-arms after firing (with a server-side re-trigger cooldown to prevent every-tick spam)."
        },
        "cancel_label": {
          "type": "string",
          "description": "ALTERNATE USE: if present (and condition/actions absent), cancels the standing order with this label instead of creating one. 'all' cancels everything."
        }
      }
    },

    "condition": {
      "description": "The condition grammar for standing orders. Deliberately small: a single comparison, or AND/ANY of comparisons (one level deep, no nesting of groups in v1).",
      "oneOf": [
        { "$ref": "#/definitions/comparison" },
        {
          "type": "object",
          "required": ["all"],
          "properties": {
            "all": {
              "type": "array",
              "minItems": 2,
              "maxItems": 3,
              "items": { "$ref": "#/definitions/comparison" }
            }
          }
        },
        {
          "type": "object",
          "required": ["any"],
          "properties": {
            "any": {
              "type": "array",
              "minItems": 2,
              "maxItems": 3,
              "items": { "$ref": "#/definitions/comparison" }
            }
          }
        }
      ]
    },

    "comparison": {
      "type": "object",
      "required": ["metric", "op", "value"],
      "properties": {
        "metric": {
          "enum": [
            "enemy_range",
            "enemy_on_sensors",
            "missile_inbound",
            "nearest_missile_range",
            "own_hull_percent",
            "own_speed",
            "own_missiles_remaining",
            "own_decoys_remaining",
            "enemy_bearing_off_nose",
            "distance_from_zone_center",
            "time_elapsed_seconds"
          ],
          "description": "enemy_bearing_off_nose = absolute degrees between our facing and the bearing to the enemy (0 = dead ahead); useful for 'when he's in our sights'. missile_inbound and enemy_on_sensors are booleans (use op=eq, value=true/false)."
        },
        "op": { "enum": ["lt", "lte", "gt", "gte", "eq"] },
        "value": {
          "type": ["number", "boolean"]
        }
      },
      "examples": [
        { "metric": "enemy_range", "op": "lt", "value": 3000 },
        { "metric": "missile_inbound", "op": "eq", "value": true },
        { "metric": "own_hull_percent", "op": "lt", "value": 30 }
      ]
    },

    "query": {
      "description": "Read-only question answered by the first mate from current game state. Server returns a state snapshot scoped to the topic; the LLM phrases the answer in character. Never mutates state, never reveals information the ship's sensors don't have.",
      "type": "object",
      "required": ["topic"],
      "properties": {
        "topic": {
          "enum": [
            "enemy",
            "own_ship",
            "weapons",
            "missiles_inbound",
            "standing_orders",
            "zone",
            "full_report"
          ]
        }
      },
      "examples": [
        { "topic": "enemy" },
        { "topic": "weapons" }
      ]
    }
  },

  "server_constants_suggested": {
    "description": "Starting tunables. These live server-side, not in the schema, but are documented here so the LLM prompt and sim agree on the world's shape. All units abstract: distance in meters, speed in m/s, angles in degrees.",
    "tick_rate_hz": 1,
    "map": {
      "zone_radius_m": 20000,
      "outside_zone_effect": "ship fully visible to enemy; own sensor range halved (soft tether, no hard wall)"
    },
    "ship": {
      "max_speed_mps": 300,
      "accel_at_full_thrust_mps2": 15,
      "turn_rate_deg_per_sec": 20,
      "hull_points": 100,
      "sensor_range_m": 12000
    },
    "laser": {
      "range_m": 5000,
      "beam_width_degrees": 4,
      "cooldown_seconds": 4,
      "damage": 10,
      "damage_vs_missile": "instant kill"
    },
    "missile": {
      "magazine": 6,
      "speed_mps": 450,
      "launch_delay_ticks": 2,
      "acquisition_cone_degrees": 60,
      "proximity_fuse_m": 150,
      "damage": 35
    },
    "decoy": {
      "supply": 4,
      "lifetime_seconds": 20
    }
  },

  "example_translations": {
    "description": "Reference examples for the LLM system prompt: captain utterance -> emitted commands.",
    "examples": [
      {
        "captain": "Flank speed, come left forty",
        "commands": [
          { "verb": "set_thrust", "params": { "percent": 100 }, "acknowledgement": "Flank speed, aye." },
          { "verb": "set_heading", "params": { "mode": "relative", "direction": "port", "degrees": 40 }, "acknowledgement": "Coming port forty." }
        ]
      },
      {
        "captain": "Point us at him and fire when ready",
        "commands": [
          { "verb": "set_heading", "params": { "mode": "target", "target": "enemy_ship" }, "acknowledgement": "Coming about to face the contact." },
          { "verb": "set_standing_order", "params": {
              "label": "fire when ready",
              "condition": { "all": [
                { "metric": "enemy_bearing_off_nose", "op": "lt", "value": 4 },
                { "metric": "enemy_range", "op": "lt", "value": 5000 }
              ]},
              "actions": [ { "verb": "fire_laser", "params": {} } ],
              "repeat": false
          }, "acknowledgement": "Guns will fire as she bears." }
        ]
      },
      {
        "captain": "If a missile comes at us, turn into it and shoot it down",
        "commands": [
          { "verb": "set_standing_order", "params": {
              "label": "missile defense",
              "condition": { "metric": "missile_inbound", "op": "eq", "value": true },
              "actions": [
                { "verb": "set_heading", "params": { "mode": "target", "target": "nearest_missile" } },
                { "verb": "fire_laser", "params": {} }
              ],
              "repeat": true
          }, "acknowledgement": "Point defense doctrine set, Captain." }
        ]
      },
      {
        "captain": "Go dark and drift",
        "commands": [
          { "verb": "set_thrust", "params": { "percent": 0 }, "acknowledgement": "Engines cold. Running silent." }
        ]
      },
      {
        "captain": "How far out is he?",
        "commands": [
          { "verb": "query", "params": { "topic": "enemy" }, "acknowledgement": "" }
        ]
      },
      {
        "captain": "Belay the missile defense order",
        "commands": [
          { "verb": "set_standing_order", "params": { "cancel_label": "missile defense" }, "acknowledgement": "Missile defense doctrine cancelled." }
        ]
      }
    ]
  },

  "llm_translator_rules": {
    "description": "Behavioral rules to bake into the LLM translator's system prompt.",
    "rules": [
      "Emit only valid commands from this schema. If the captain's intent maps to no verb, emit no command and have the acknowledgement explain what the ship can and cannot do.",
      "Ambiguity policy: make a reasonable interpretation and state it in the acknowledgement rather than asking clarifying questions. Misinterpretation is part of the game; stalling is not.",
      "Compound utterances may emit up to 4 commands, executed in order.",
      "Never invent game state. Use query for anything the captain asks about; the acknowledgement for a query is written AFTER the server returns the snapshot.",
      "Conditional phrasing ('if', 'when', 'as soon as', 'the moment') always becomes set_standing_order, never immediate execution.",
      "Vague quantities get sensible defaults, stated aloud: 'slow down' -> thrust 25%, 'ease off' -> reduce by ~25 points, 'hard about' -> relative 180."
    ]
  }
}
